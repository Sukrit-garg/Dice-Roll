import 'package:flutter/material.dart';
class StyledText extends StatelessWidget{
  const StyledText({super.key});
  @override
  Widget build(context){
    return const Text(
              "Suraj lag raha hu",style: TextStyle(color: Colors.white, fontSize: 28),
            );
  }
}